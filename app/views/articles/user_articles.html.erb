<% unless @articles.empty? %>
  <% @articles.each do |article| %>
    <h1><%= link_to article.title, article %></h1>
  <% end %>
<% else %>
  <h4>No articles yet? Let's <%= link_to 'change', new_article_path %> that!</h4>
<% end %>
