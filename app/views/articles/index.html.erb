<h1>Articles!</h1>


this blog has <strong><%= @articles.public_count %></strong> articles and counting! 
<% @articles.each do |article| %>
  <% unless article.archived? %>
    <li><%= link_to "#{article.user.name} - #{article.title}" , article %></li>
  <% end %>
<% end %>

<%= link_to "New Article", new_article_path %>
